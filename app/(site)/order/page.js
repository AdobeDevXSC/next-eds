export const metadata = { title: 'Your order — Stacked' };

// Placeholder — the order view is built in step (e) of the redesign plan.
export default function OrderPage() {
  return (
    <main style={{ padding: '40px 22px', maxWidth: 720, margin: '0 auto' }}>
      <h1 style={{ fontFamily: 'var(--font-content)', fontStyle: 'italic' }}>Your order</h1>
      <p style={{ color: 'var(--text-secondary)' }}>Order view coming soon.</p>
    </main>
  );
}
