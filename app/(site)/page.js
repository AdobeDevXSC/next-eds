export const metadata = {
  title: 'Stacked — build your lunch, brick by brick',
  description: 'A sandwich counter with two ways to order: shop a signature menu or build your own, brick by brick.',
};

// Placeholder — the full 2a/3a home is built in step (c) of the redesign plan.
export default function HomePage() {
  return (
    <main style={{ padding: '40px 22px', maxWidth: 720, margin: '0 auto' }}>
      <h1 style={{ fontFamily: 'var(--font-content)', fontStyle: 'italic' }}>Home</h1>
      <p style={{ color: 'var(--text-secondary)' }}>Redesigned home coming next.</p>
    </main>
  );
}
